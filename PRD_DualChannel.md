# WeChat Smart Bot - 双通道架构设计

## 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                    用户 / 客户                           │
└────────────┬────────────────────────────┬───────────────┘
             │                            │
             ▼                            ▼
┌────────────────────────┐  ┌─────────────────────────────┐
│   通道1: iLink Bot     │  │  通道2: 个人微信账号          │
│   (AI 客服机器人)       │  │  (自然人设, 自动回复)         │
│                        │  │                             │
│  - 主动表明 AI 身份     │  │  - 不暴露 AI 身份            │
│  - 引导式对话           │  │  - 自然语言回复              │
│  - 选项菜单            │  │  - 转发给监控账户             │
│  - 专业客服风格         │  │  - 关键问题人工确认           │
│                        │  │                             │
│  技术: weixin-bot-sdk  │  │  技术: Computer Use          │
│  (iLink Bot API)       │  │  (截图+OCR+键盘模拟)         │
└────────────┬───────────┘  └──────────┬──────────────────┘
             │                          │
             ▼                          ▼
┌─────────────────────────────────────────────────────────┐
│              共享 LLM 层 (SenseNova API)                 │
│         - 智能回复生成                                    │
│         - 消息分类判断                                    │
│         - 对话上下文管理                                  │
└────────────────────────┬────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────┐
│              监控账户 (musomuso)                          │
│         - 接收两个通道的消息转发                           │
│         - 关键问题确认                                    │
│         - 人工介入                                        │
└─────────────────────────────────────────────────────────┘
```

---

## 通道1: iLink Bot (AI 客服)

### 定位
- **身份**: 明确表明是 AI 智能助手
- **场景**: 客户主动添加机器人后的标准化服务
- **风格**: 专业、高效、有引导性

### System Prompt 设定
```
你是 [品牌名] 的 AI 智能客服助手。

核心规则：
1. 开场白: "你好！我是 [品牌名] 的 AI 客服助手，有什么可以帮您？"
2. 主动提供选项引导对话，例如：
   - "您可以问我：产品咨询 | 价格查询 | 售后服务 | 其他问题"
3. 回复简洁专业，每次回复控制在 3 句以内
4. 涉及价格/合同/投诉时，回复："这个问题我需要转接人工客服，请稍等"
5. 不确定的问题: "让我为您查询一下..."
```

### 技术实现
```python
# 使用 weixin-bot-sdk
from weixin_bot import WeixinBot

bot = WeixinBot()
bot.login()

@bot.on_message
async def handle(msg):
    await bot.send_typing(msg.user_id)  # 显示"正在输入"
    reply = generate_reply(msg.text, context="official_bot")
    await bot.reply(msg, reply)
    # 转发给监控账户
    forward_to_monitor(msg.user_id, msg.text, reply)

bot.run()
```

### 关键 API 端点
| 端点 | 方法 | 说明 |
|------|------|------|
| `/get_bot_qrcode` | GET | 获取登录二维码 |
| `/getupdates` | POST | 长轮询接收消息 |
| `/sendmessage` | POST | 发送消息 |
| `/sendtyping` | POST | 显示输入状态 |
| `/withdrawmessage` | POST | 撤回消息 |

---

## 通道2: 个人微信 (自然人设)

### 定位
- **身份**: 不暴露 AI，像真人一样回复
- **场景**: 已有联系人通过个人号沟通
- **风格**: 自然、亲切、不机械

### System Prompt 设定
```
你是一个真人在回复微信消息。请注意：
1. 绝对不要暴露你是 AI 或机器人
2. 不要说"作为 AI 助手"之类的话
3. 语气要自然，像朋友聊天
4. 回复简短，一般 1-2 句话
5. 可以用"哈哈"、"嗯嗯"等口语化表达
6. 不确定时说"我看看"、"稍等我查一下"
7. 绝不主动提供建议或选项菜单
```

### 技术实现
- 使用 Computer Use 方式（截图 + OCR + 键盘模拟）
- 每 3 秒扫描聊天列表
- 检测到新消息 → OCR 读取 → LLM 生成自然回复 → 键盘发送
- 所有消息转发给 musomuso

---

## 消息转发规则

### 转发格式
```
📨 [来源: 通道1/通道2]
👤 联系人: [名称]
⏰ 时间: [时间]
📝 内容: [消息内容]
🤖 回复: [AI 回复内容]
📊 状态: [已自动回复/待确认]
```

### 关键问题检测
| 检测方式 | 适用通道 | 说明 |
|----------|----------|------|
| 关键词匹配 | 两个通道 | 投诉、退款、赔偿等 |
| LLM 语义判断 | 两个通道 | 价格、合同、敏感话题 |
| 金额检测 | 两个通道 | 涉及具体金额的消息 |

### 确认流程
1. 检测到关键问题 → 暂停自动回复
2. 转发给 musomuso（标注"待确认"）
3. musomuso 回复确认指令：
   - `1` = 使用 AI 建议回复
   - `2+内容` = 自定义回复
   - `3` = 暂不回复

---

## 文件结构

```
wechat-smart-bot/
├── config.py               # 配置管理
├── database.py             # SQLite 存储
├── llm_client.py           # SenseNova LLM
├── channel1_bot.py         # 通道1: iLink Bot
├── channel2_monitor.py     # 通道2: Computer Use 监控
├── message_router.py       # 消息路由
├── forwarder.py            # 消息转发给 musomuso
├── prd/
│   ├── PRD.md              # 产品需求文档
│   ├── PRD_DualChannel.md  # 双通道架构
│   └── PRD_ComputerUse.md  # Computer Use 能力
└── data/                   # 数据存储
```

---

## 实施优先级

| 步骤 | 内容 | 优先级 |
|------|------|--------|
| 1 | 安装 weixin-bot-sdk | P0 |
| 2 | 实现通道1 (iLink Bot) | P0 |
| 3 | 测试 Bot 登录和消息收发 | P0 |
| 4 | 优化通道2 (Computer Use) OCR | P1 |
| 5 | 实现消息转发给 musomuso | P1 |
| 6 | 关键问题检测 + 人工确认 | P2 |
| 7 | Web 管理面板（可选） | P3 |
