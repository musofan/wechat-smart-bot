# WeChat Computer Use Skills - 能力清单与规则文档

## 1. 当前已具备的能力

### 1.1 窗口管理
| 能力 | 状态 | 说明 |
|------|------|------|
| 定位微信窗口 | ✅ | 通过 `Qt51514QWindowIcon` 类名定位 Weixin 4.x |
| 窗口置顶 | ✅ | ShowWindow + SetForegroundWindow |
| 获取窗口坐标 | ✅ | GetWindowRect 获取精确位置 |
| 窗口尺寸自适应 | ✅ | 所有坐标基于窗口比例计算 |

### 1.2 屏幕截图与识别
| 能力 | 状态 | 说明 |
|------|------|------|
| 全窗口截图 | ✅ | pyautogui.screenshot |
| 区域截图 | ✅ | 支持相对坐标区域截图 |
| OCR 文字识别 | ✅ | EasyOCR 中英文识别 |
| 未读消息检测 | ✅ | 通过红色像素检测未读角标 |

### 1.3 消息交互
| 能力 | 状态 | 说明 |
|------|------|------|
| 读取聊天消息 | ✅ | 截图 + OCR 识别消息内容 |
| 读取联系人名 | ✅ | OCR 识别聊天标题 |
| 发送文本消息 | ✅ | 剪贴板粘贴 + Enter 发送 |
| 搜索联系人 | ✅ | 点击搜索框 + 输入名字 |
| 打开对话 | ✅ | 点击聊天列表项 |

### 1.4 智能处理
| 能力 | 状态 | 说明 |
|------|------|------|
| LLM 智能回复 | ✅ | SenseNova API |
| 对话上下文管理 | ✅ | SQLite 存储历史对话 |
| 消息分类 | ✅ | 关键词 + LLM 语义判断 |
| 关键词检测 | ✅ | 可配置敏感词列表 |

---

## 2. 待开发的能力

### 2.1 UI 导航技能
| 能力 | 优先级 | 说明 |
|------|--------|------|
| 下拉菜单操作 | P1 | 识别并点击下拉菜单项 |
| 右键菜单 | P1 | 模拟右键打开上下文菜单 |
| 滚动列表 | P1 | 在聊天列表中上下滚动 |
| Tab 切换 | P2 | 在不同面板间切换 |
| 弹窗处理 | P1 | 关闭/确认各种弹窗 |

### 2.2 搜索与定位
| 能力 | 优先级 | 说明 |
|------|--------|------|
| 联系人搜索 | P0 | 搜索并打开指定联系人 |
| 群聊搜索 | P1 | 搜索并打开指定群聊 |
| 聊天记录搜索 | P2 | 在当前对话中搜索历史消息 |
| 通讯录浏览 | P2 | 浏览通讯录查找联系人 |

### 2.3 多媒体处理
| 能力 | 优先级 | 说明 |
|------|--------|------|
| 图片消息识别 | P2 | 识别图片内容（需视觉 LLM） |
| 文件消息处理 | P2 | 识别文件类型和名称 |
| 语音消息 | P3 | 暂不支持（需要 ASR） |
| 表情消息 | P3 | 暂不支持 |

### 2.4 高级操作
| 能力 | 优先级 | 说明 |
|------|--------|------|
| 转发消息 | P1 | 长按消息 → 转发 → 选择联系人 |
| 群@某人 | P2 | 在群聊中 @特定成员 |
| 发送名片 | P2 | 发送个人/公众号名片 |
| 修改备注 | P3 | 修改联系人备注名 |

---

## 3. 应答规则

### 3.1 自动回复规则
```
规则1: 所有文本消息都应自动回复
规则2: 回复内容由 LLM 根据上下文生成
规则3: 回复前检查是否命中敏感关键词
规则4: 命中敏感词 → 暂不回复，转发给监控账户确认
规则5: 回复延迟 1-3 秒（模拟人类）
```

### 3.2 敏感词规则
```
触发人工确认的关键词:
- 投诉、退款、赔偿、法律、律师
- 合同终止、重大问题、取消订单
- 价格、报价、费用（LLM 判断）
- 负面情绪（LLM 判断）
```

### 3.3 转发规则
```
规则1: 所有收到的消息都转发给监控账户 (musomuso)
规则2: 转发格式: 发送者 + 时间 + 内容 + AI回复
规则3: 需要确认的消息标注"待确认"
规则4: 转发后在监控账户中标记状态
```

### 3.4 聊天列表监控规则
```
规则1: 每 5 秒扫描一次聊天列表
规则2: 检测红色未读角标
规则3: 有未读 → 点击打开 → OCR 读取 → 回复 → 转发
规则4: 跳过自己的消息
规则5: 跳过公众号消息（可配置）
规则6: 同一消息不重复处理（hash 去重）
```

---

## 4. UI 坐标参考（Weixin 4.1.10.53）

```
┌──────────────────────────────────────────┐
│  [头像]  搜索框                    [设置] │  ← 顶部栏
│──────────────────────────────────────────│
│  [聊] [通讯] [收藏] [文件] [朋友圈]      │  ← 导航栏 (x: 0-50)
│──────────────────────────────────────────│
│  ┌─────────┐ ┌──────────────────────┐    │
│  │ 搜索框  │ │                      │    │
│  │─────────│ │   聊天标题           │    │
│  │ 聊天1   │ │──────────────────────│    │
│  │ 聊天2   │ │                      │    │
│  │ 聊天3   │ │   消息区域           │    │
│  │ ...     │ │                      │    │
│  │         │ │                      │    │
│  └─────────┘ │──────────────────────│    │
│   (0-32%)    │ [输入框]        [发送]│    │  ← 输入区 (y: 88-98%)
│              └──────────────────────┘    │
│              (32-100%)                   │
└──────────────────────────────────────────┘
```

### 关键坐标（相对窗口 0-1 范围）
| 元素 | x 范围 | y 范围 |
|------|--------|--------|
| 导航栏 | 0.00 - 0.07 | 0.00 - 1.00 |
| 搜索框 | 0.07 - 0.32 | 0.03 - 0.06 |
| 聊天列表 | 0.07 - 0.32 | 0.09 - 0.95 |
| 聊天标题 | 0.35 - 0.95 | 0.00 - 0.10 |
| 消息区域 | 0.35 - 0.95 | 0.12 - 0.82 |
| 输入框 | 0.35 - 0.85 | 0.88 - 0.95 |
| 发送按钮 | 0.88 - 0.95 | 0.92 - 0.96 |

---

## 5. 已知限制

1. **OCR 精度**: 中文小字体可能识别不准确
2. **窗口遮挡**: 其他窗口覆盖微信时无法截图
3. **DPI 缩放**: 高 DPI 显示器可能需要调整坐标
4. **消息类型**: 目前只支持文本消息
5. **群消息**: 群消息中无法区分具体发送者
6. **速度**: OCR + LLM 处理需要 3-5 秒延迟

---

## 6. 更新日志

- **2026-06-22**: 初始版本，包含基础能力清单
